/* TITLE
let repeatIt = function(str, n) {
return typeof str === 'string' ? str.repeat(n) : 'Not a string'
}


URL


INSTRUCTIONS


EXAMPLES


*/
let repeatIt = function(str, n) {
  return typeof str === 'string' ? str.repeat(n) : 'Not a string'
  }
  